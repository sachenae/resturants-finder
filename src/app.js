import React, { Component } from 'react';
import {Home} from './layout';

export default class App extends Component {
    render(){
        return(
            <div>
                <Home />
            </div>
        );
    }
}